const defaultState = {
  inputVal: '',
};

export const searchInputReducer = (
  state = defaultState,
  action = {
    type: '',
  }
) => {
  switch (action.type) {
    default:
      return state;
  }
};
