const defaultState = {
  res: [],
};

export const searchResultsReducer = (
  state = defaultState,
  action = {
    type: '',
  }
) => {
  switch (action.type) {
    default:
      return state;
  }
};
